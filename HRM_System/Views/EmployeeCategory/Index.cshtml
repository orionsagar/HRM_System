@model IEnumerable<EmployeeCategory>
@{
    ViewData["Title"] = "Category List";
}

@section PageName {
    Category List
}
@section Breadcrumb {
    <a href="/employeecategory/index">
        <i class="fa fa-home"></i>
    </a>
    <span class="Divider">/</span>
    <span class="section activeCrum">Category List</span>
}
<!-- Page-body start -->
<div class="page-body">
    <div class="row">
        <div class="col-sm-12">
            <div class="card">
                <div class="card-block">
                    <div class="card-header">
                        <a asp-action="create" class="btn waves-effect waves-light btn-primary  btn-sm btn-outline-primary @ViewBag.IsAdd"><i class="icofont icofont-ui-add"></i> Add New</a>
                    </div>
                    <div class="table-responsive dt-responsive">
                        <table id="dt-category" class="table table-striped table-bordered nowrap">
                            <thead>
                                <tr>
                                    <th>Emp CatId</th>
                                    <th>Type Name</th>
                                    @* <th>Type In Bangla</th> *@
                                    <th>Description</th>
                                    <th></th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>
                                @*@foreach (var item in Model)
                    {
                    <tr>
                    <td>@item.EmpCatId</td>
                    <td>@item.Name</td>
                    <td>@item.NameGeneric</td>
                    <td>@item.Description</td>
                    <td>
                    <a asp-action="Edit" asp-route-id="@item.EmpCatId"><i class="fa fa-edit"></i></a>
                    </td>
                    <td>
                    <a asp-action="Delete" asp-route-Id="@item.EmpCatId"><i class="fa fa-trash-o"></i></a>
                    </td>
                    </tr>
                    }*@
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
            <!-- Ajax data source (Arrays) table end -->
        </div>
    </div>
</div>
<!-- Page-body end -->
<input type="hidden" id="IsView" name="IsView" value="@ViewBag.IsView" />
<input type="hidden" id="IsAdd" name="IsAdd" value="@ViewBag.IsAdd" />
<input type="hidden" id="IsDelete" name="IsDelete" value="@ViewBag.IsDelete" />
<input type="hidden" id="IsEdit" name="IsEdit" value="@ViewBag.IsEdit" />




@section Scripts {

}