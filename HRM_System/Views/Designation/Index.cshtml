@{
    ViewData["Title"] = "Designation List";
}

@section PageName {
    Designation List
}
@section Breadcrumb {
    <a href="/dashboard/index">
        <i class="fa fa-home"></i>
    </a>
    <span class="Divider">/</span>
    <span class="section activeCrum">Designation List</span>
}
@{
}

<div class="page-body">
    <div class="row">
        <div class="col-sm-12">
            <!-- Ajax data source (Arrays) table start -->
            <div class="card">
                <div class="card-header">
                    <a asp-action="Create" class="btn waves-effect waves-light btn-primary  btn-sm btn-outline-primary @ViewBag.IsAdd"><i class="icofont icofont-ui-add"></i> Add New</a>
                </div>
                <div class="card-block ">
                    <div class="table-responsive dt-responsive">
                        <table id="dt-designation" class="table table-striped table-bordered nowrap">
                            <thead>
                                <tr>
                                    <th>desigId</th>
                                    <th>Desig Name</th>
                                    @*<th>In Bangla</th>*@
                                    @*<th>Pay Scale</th>
                                    <th>Grade/Scale</th>*@
                                    <th>Description</th>
                                    <th></th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
            <!-- Ajax data source (Arrays) table end -->
        </div>
    </div>
</div>

<input type="hidden" id="IsView" name="IsView" value="@ViewBag.IsView" />
<input type="hidden" id="IsAdd" name="IsAdd" value="@ViewBag.IsAdd" />
<input type="hidden" id="IsDelete" name="IsDelete" value="@ViewBag.IsDelete" />
<input type="hidden" id="IsEdit" name="IsEdit" value="@ViewBag.IsEdit" />

@section Scripts {

}