@model IEnumerable<BillingMasterVM>
@{
    ViewData["Title"] = "Payment Receipt List";
}

@section PageName {
    Payment Receipt
}
@section Breadcrumb {
    <a href="/department/index">
        <i class="fa fa-home"></i>
    </a>
    <span class="Divider">/</span>
    <span class="section activeCrum">Payment Receipt</span>
}

<div class="page-body">
    <div class="row">
        <div class="col-sm-12">
            <div class="card">
                <div class="card-block">
                    <div class="table-responsive dt-responsive">
                        <table id="dt-pr" class="table table-striped table-bordered nowrap">
                            <thead>
                                <tr>
                                    <th>PRMasterId</th>
                                    <th>SL No</th>
                                    <th></th>
                                    <th>Invoice No</th>
                                    <th>Billing Amount</th>
                                    <th>Bill Date</th>
                                    <th>Receipt Amount</th>
                                    <th>Payment Date</th>
                                    <th>Status</th>
                                    <th>Description</th>
                                    @*<th>Payment Receipt</th>*@
                                </tr>
                            </thead>
                            <tbody>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<input type="hidden" id="IsView" name="IsView" value="@ViewBag.IsView" />
<input type="hidden" id="IsAdd" name="IsAdd" value="@ViewBag.IsAdd" />
<input type="hidden" id="IsDelete" name="IsDelete" value="@ViewBag.IsDelete" />
<input type="hidden" id="IsEdit" name="IsEdit" value="@ViewBag.IsEdit" />




@section Scripts {

}